export default function AccentColorIcon() {
    return (
        <image
            css={`
                color: var(--accent-color);
            `}
            iconName={"preferences-color-symbolic"}
            pixelSize={16}
        />
    )
}
